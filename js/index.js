document.addEventListener('DOMContentLoaded', () => {
    const caseTop = document.getElementById('caseTop');
    const powerButton = document.getElementById('powerButton');

    setSize(caseTop, "100%");
    setPosition(caseTop, "50%", "50%");

    setSize(powerButton, "7vw");
    setPosition(powerButton, "24.8%", "47.9%");

    powerButton.addEventListener("click", function(e) {
        
    });
});